    <header class="header">
      <div class='site-title'>
        <%= link_to 'サイトタイトル', root_path %>
      </div>
      <% if user_signed_in? %>
        <div class='right-btn'>
          <%= current_user.nickname %>の日記
          <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: :logout %>
          <%= link_to "日記作成", new_diary_path, class: :header_btn %>
        </div>
      <% end %>
    </header>